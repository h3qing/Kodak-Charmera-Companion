{
  "identifier": "default",
  "description": "Capabilities for the main window. Kept minimal on purpose: the webview only needs to open native file/folder pickers. All filesystem reads and writes happen in Rust, behind commands that validate their paths.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-save",
    "dialog:allow-open"
  ]
}
